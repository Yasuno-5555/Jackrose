# v0.35.4 Controlled Mutation Re-Enablement

This release adds a controlled mutation test path without reopening the general installer flow.

Highlights:

- mutation test mode remains disabled by default
- disposable target enforcement is mandatory
- plan hashing and explicit confirmation are mandatory
- helper mutation re-checks the signed plan before execution
- post-mutation verification and reporting remain required
