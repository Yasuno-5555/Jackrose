# Rescue Kernel Check

- Inspect kernel, initramfs, and bootloader files with `scripts/cidre-rescue-kernel-check`
- Review pacman logs before making changes
- Use read-only inspection first
- v0.26.0 suggests repair commands but does not perform repair automatically
- If the system is beyond practical repair, export state and consider reinstall or exit
