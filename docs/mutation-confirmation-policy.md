# Mutation Confirmation Policy

Mutation execution requires typed confirmation.

Required phrase:

`I understand this can destroy the selected disposable target.`

The phrase is converted into a short-lived confirmation token bound to the exact plan hash.
