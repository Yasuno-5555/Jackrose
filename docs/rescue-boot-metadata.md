# Rescue Boot Metadata

- schema versioned JSON
- fields include slot type, profile, boot integration state, and validation state
- validate with `scripts/cidre-rescue-slot-metadata --validate`
- related but separate from the rescue rootfs manifest
- intended for future installer and boot integration flows
