# Maintainer: Yasuno <yasuno@example.invalid>

pkgname=jackrose-pack-security-gui
pkgver=1.0.0
pkgrel=1
pkgdesc='Jackrose Workload Pack for Graphical Security applications'
url='https://github.com/Yasuno-5555/Jackrose'
arch=('any')
license=('custom')
depends=(
  'jackrose-pack-security'
  # Deferred candidates (not included in hard depends due to aarch64 build fragility):
  # 'caido'
  # 'owasp-zap'
  # 'wireshark'
)
source=()
sha256sums=()

package() {
  true
}
