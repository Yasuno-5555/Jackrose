# Maintainer: Yasuno <yasuno@example.invalid>

pkgname=cidre-user-setup
pkgver=1.0.0
pkgrel=1
pkgdesc='Cidre user environment setup tool'
url='https://github.com/Yasuno-5555/Cidre'
arch=('any')
license=('custom')
depends=('bash')
source=('cidre-user-setup')
sha256sums=('SKIP')

package() {
  install -Dm755 "${srcdir}/cidre-user-setup" "${pkgdir}/usr/bin/cidre-user-setup"
}
