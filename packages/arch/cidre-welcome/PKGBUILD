# Maintainer: Yasuno <yasuno@example.invalid>

pkgname=cidre-welcome
pkgver=1.0.0
pkgrel=1
pkgdesc='Cidre first-login welcome dashboard screen'
url='https://github.com/Yasuno-5555/Cidre'
arch=('any')
license=('custom')
depends=('bash')
source=()
sha256sums=()

# Monorepo-local packaging path.
# This will be replaced by source=() when packages move to cidre-packages.
_repo_root="${startdir}/../../.."

package() {
  install -Dm755 "${_repo_root}/components/welcome/bin/cidre-welcome" "${pkgdir}/usr/bin/cidre-welcome"
}

