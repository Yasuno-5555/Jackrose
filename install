#!/bin/sh
# install: Root-level entrypoint script to launch Cidre guided installer
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec "${SCRIPT_DIR}/scripts/cidre-installer" "$@"
