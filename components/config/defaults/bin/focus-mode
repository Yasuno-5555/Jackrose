#!/usr/bin/env bash
set -euo pipefail
mkdir -p "$HOME/.cache"
printf 'focus\n' > "$HOME/.cache/niri-current-profile"
swaync-client -dn true >/dev/null 2>&1 || true
notify-send "Jackrose" "Focus mode active."
