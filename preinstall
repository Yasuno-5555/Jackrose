#!/bin/sh
# preinstall: Root-level entrypoint script to launch Cidre preinstall helper
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec "${SCRIPT_DIR}/scripts/cidre-preinstall" "$@"
